Ddoc

$(P Pc4d is a parser combinator library for $(LINK2 https://dlang.org, dlang))
$(P )

$(P The library comes with a small set of parsers that can be combined to build bigger parser:)
<%= parsers %>

Macros:
        TITLE=Pc4d
        DDOC_BLANKLINE=
_=
